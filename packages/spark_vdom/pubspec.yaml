name: spark_vdom
description: Virtual DOM engine for Spark - browser-side mounting, patching, and hydration.
version: 1.0.0-alpha.2
resolution: workspace

homepage: https://spark.kleak.dev
repository: https://github.com/KLEAK-Development/spark
issue_tracker: https://github.com/KLEAK-Development/spark/issues

environment:
  sdk: ^3.10.0

dependencies:
  spark_html_dsl: ^1.0.0-alpha.1
  spark_web: ^1.0.0-alpha.3
  meta: ^1.18.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
